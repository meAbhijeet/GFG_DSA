class Solution{
    public:
    int digitsInFactorial(int N)
    {
        // code here
        double count=0;
       
       for (int i=2; i<=N; i++)
        count += log10(i);
 
    return floor(count) + 1;
    }
};
